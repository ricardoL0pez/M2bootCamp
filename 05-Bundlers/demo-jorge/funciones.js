export const suma = (a, b) => a + b;

export const resta = (a, b) => a + b;

/* module.exports = {
    suma,
    resta,
} */
//En back end trabajamos con el module.exports


/* objetos literales
decir:
objeto = {
    suma : suma;
} 
es igual:
objeto = {
    suma;
}   shift + alt + A */

//export default para modulos con una sola funcion, ya que a esta no es necesario hacer destructuring